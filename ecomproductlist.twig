{% extends "layout.twig" %}
{% block content %}

<section class="template template__shop">
	{% include "includes/header.twig" %}
</section>

<section class="page page__ecomproduct">
    <div class="template__content">
        <div class="container">
			{{widget('ecombasket', 'shopbasket', {})|raw}}
			{{widget('pagetitle', 'title', {})|raw}}
            {{zone('main')|raw}}
			{{widget('space', 'pagespace', {'height': '20'})|raw}}
        </div>
    </div>
</section>

{% include "includes/footer.twig" %}

{% endblock %} 